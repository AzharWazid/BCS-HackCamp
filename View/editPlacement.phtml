<?php require_once("template/header.phtml"); ?>

<head>
    <style>
        .form-container {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }

        .form-container h1 {
            margin-bottom: 20px;
        }

        .form-container label {
            font-weight: bold;
            margin-top: 10px;
            display: block;
        }

        .form-container input, .form-container textarea {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .form-container button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .form-container button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<div class="container my-5">
    <div class="form-container">
        <h1>Edit Placement</h1>
        <form action="../Controller/updatePlacement.php" method="POST">
            <!-- Input fields for placement details -->
            <div>
                <label for="title">Job Title:</label>
                <input type="text" id="title" name="title" value="<?= htmlspecialchars($_POST['title'] ?? ''); ?>" required>
            </div>

            <div>
                <label for="description">Description:</label>
                <textarea id="description" name="description" rows="5" required><?= htmlspecialchars($_POST['description'] ?? ''); ?></textarea>
            </div>

            <div>
                <label for="salary">Salary (£):</label>
                <input type="number" id="salary" name="salary" value="<?= htmlspecialchars($_POST['salary'] ?? ''); ?>" required>
            </div>

            <div>
                <label for="location">Location:</label>
                <input type="text" id="location" name="location" value="<?= htmlspecialchars($_POST['location'] ?? ''); ?>" required>
            </div>

            <div>
                <label for="skills">Skills Required:</label>
                <input type="text" id="skills" name="skills" value="<?= htmlspecialchars($_POST['skills'] ?? ''); ?>" required>
            </div>

            <div>
                <label for="start_date">Start Date:</label>
                <input type="date" id="start_date" name="start_date" value="<?= htmlspecialchars($_POST['start_date'] ?? ''); ?>" required>
            </div>

            <div>
                <label for="end_date">End Date:</label>
                <input type="date" id="end_date" name="end_date" value="<?= htmlspecialchars($_POST['end_date'] ?? ''); ?>" required>
            </div>

            <!-- Hidden field for the placement ID -->
            <input type="hidden" name="placement_id" value="<?= htmlspecialchars($_GET['id'] ?? ''); ?>">

            <!-- Submit button -->
            <button type="submit">Save Changes</button>
        </form>
    </div>
</div>
<?php require_once("template/footer.phtml"); ?>
</body>